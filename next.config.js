/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  sassOptions: {
    includePaths: [],
  },
  images: {
    unoptimized: false,
  },
}

module.exports = nextConfig

